// This is a test for custom modules
// It could be filled out to test the functionality that deltaAddPartLink 
// requires, but for now is basically just testing that it is being included.
(function() {
    // This assumes that 'app.custom' has been created within app.js (or similar)
    
})();